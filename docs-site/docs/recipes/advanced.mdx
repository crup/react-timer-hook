---
title: Advanced recipes
description: Live examples for many timers, item schedules, global controls, and dynamic timer lists.
---

import RecipePlayground from '@site/src/components/RecipePlayground';
import { ManyDisplayCountdownsSample, TimerGroupSample, GroupControlsSample, PerItemPollingSample, DynamicItemsSample } from '../../samples/recipes';

# Advanced recipes

## 11. Many display-only countdowns

<RecipePlayground title="Many display-only countdowns">
  <ManyDisplayCountdownsSample />
</RecipePlayground>

Use one shared `useTimer()` when rows only need display countdowns.

## 12. Many independent timers

<RecipePlayground title="Timer group">
  <TimerGroupSample />
</RecipePlayground>

Use `useTimerGroup()` when every row needs independent lifecycle controls.

## 13. Group-level pause and resume

<RecipePlayground title="Group controls">
  <GroupControlsSample />
</RecipePlayground>

```tsx
timers.pauseAll();
timers.resumeAll();
timers.restartAll();
```

## 14. Per-item polling

<RecipePlayground title="Per-item polling">
  <PerItemPollingSample />
</RecipePlayground>

Each group item can define its own schedules.

## 15. Add and remove timers dynamically

<RecipePlayground title="Dynamic timer items">
  <DynamicItemsSample />
</RecipePlayground>

```tsx
timers.add({ id, autoStart: true });
timers.remove(id);
```
