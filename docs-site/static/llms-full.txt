# @crup/react-timer-hook full AI context

## Install

```sh
npm install @crup/react-timer-hook@alpha
pnpm add @crup/react-timer-hook@alpha
```

## Exports

```ts
import { durationParts, useTimer, useTimerGroup } from '@crup/react-timer-hook';
```

## useTimer

```ts
useTimer({
  autoStart?: boolean;
  updateIntervalMs?: number;
  endWhen?: (snapshot: TimerSnapshot) => boolean;
  onEnd?: (snapshot: TimerSnapshot, controls: TimerControls) => void | Promise<void>;
  schedules?: TimerSchedule[];
  debug?: TimerDebug;
})
```

Use `now` for wall-clock countdowns and clocks. Use `elapsedMilliseconds` for stopwatches and pausable duration countdowns.

## useTimerGroup

Use `useTimerGroup()` for many keyed timers that each need independent lifecycle controls.

## Schedules

Schedules are app-owned side effects. Default overlap behavior is `skip`.

## Recipes index

Basic:
- Wall-clock display: `new Date(timer.now)`.
- Stopwatch: render `timer.elapsedMilliseconds`.
- Absolute deadline countdown: `Math.max(0, expiresAt - timer.now)`.
- Pausable duration countdown: `durationMs - timer.elapsedMilliseconds`.
- Manual timer: use `start`, `restart`, and `reset`.

Intermediate:
- Once-only end action: `onEnd`.
- Poll while active: `schedules`.
- Poll and cancel early: call `controls.cancel(reason)`.
- Backend event stop: call `timer.cancel(reason)` from a subscription.
- Debug logs: opt in with `debug.logger`.

Advanced:
- Many display-only countdowns: one shared `useTimer`.
- Many independent timers: `useTimerGroup`.
- Group pause/resume: `pauseAll` and `resumeAll`.
- Per-item polling: item `schedules`.
- Dynamic item lifecycle: `add`, `update`, `remove`.

## MCP

Local docs MCP server:

```json
{
  "mcpServers": {
    "react-timer-hook-docs": {
      "command": "node",
      "args": ["/absolute/path/to/react-timer-hook/mcp/server.mjs"]
    }
  }
}
```

## Non-goals

No formatting, no timezone handling, no mode enums, no data-fetching cache, and no raw timeout handle exposure.
